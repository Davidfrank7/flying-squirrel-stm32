# APP/CMakeLists.txt
set(APP_SOURCES 
    ${CMAKE_CURRENT_LIST_DIR}/main.cpp)

if(BSP_USE_IMU)
    message("BSP_USE_IMU is enabled")
    list(APPEND APP_SOURCES ${CMAKE_CURRENT_LIST_DIR}/imu_thread.cpp)
endif()

if(BSP_USE_TOF)
    message("BSP_USE_TOF is enabled")
    list(APPEND APP_SOURCES ${CMAKE_CURRENT_LIST_DIR}/tof_thread.cpp)
endif()

# 如果有其他头文件，可以设置包含目录
set(APP_INCLUDE_DIRS ${CMAKE_CURRENT_LIST_DIR})

message("./${APP_SOURCES}: " ${APP_SOURCES})
message("./{APP_INCLUDE_DIRS}: " ${APP_INCLUDE_DIRS})
